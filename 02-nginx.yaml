- name: Installing Nginx
  hosts: WEB
  become: yes
- tasks:
  - name: Install Nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: Start Service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
